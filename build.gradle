allprojects {
  buildscript {
    repositories {
      mavenCentral()
    }
  }

  dependencies {
    repositories {
      mavenCentral()
    }
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.7'
}
